### Install
Create symlinks
```
stow -v vim
stow -v pdb
stow -v tmux
```

Create directories
```
mkdir ~/.swp
mkdir ~/.backups
```


TODO:
```
mv ~/.bashrc ~/.bashrc.local
mv ~/.gitconfig ~/.gitconfig.local
mv ~/.gitignore ~/.gitignore.local
```
